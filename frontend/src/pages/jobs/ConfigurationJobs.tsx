import { Typography } from 'antd';

const { Title } = Typography;

export const ConfigurationJobs = () => {
  return (
    <div>
      <Title level={2}>Configuration Jobs</Title>
      <p>This page will display configuration jobs.</p>
    </div>
  );
};

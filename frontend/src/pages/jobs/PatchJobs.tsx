import { Typography } from 'antd';

const { Title } = Typography;

export const PatchJobs = () => {
  return (
    <div>
      <Title level={2}>Patch Jobs</Title>
      <p>This page will display patch jobs.</p>
    </div>
  );
};
